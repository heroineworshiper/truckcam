# copy this to /sdcard/truckcam/

# all parameters are 0-100 percent of screen width
# don't move if face is within this distance of the goal position
DEADBAND 5
# PID factor
SPEED 100


# face size is ignored if these parameters are met
# set any parameter to 0 to always use face size
# select face if within this radius of the previous position
XY_RADIUS 10

# select face if within this percentage of the previous size
SIZE_RADIUS 50

# select face if within this percentage of the previous color
COLOR_RADIUS 10










